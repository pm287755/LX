name: blank

on:
  push:

jobs:
  exciting:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        exploit: ["qualification", "unaffordable", "aware", "approximately", "thunder"]
        huge: ["announcer", "confess", "me", "aggressive"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar pS
